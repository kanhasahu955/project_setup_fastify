# ===========================================
# Server Configuration
# ===========================================
PORT=8000
HOST=0.0.0.0
NODE_ENV=development

# ===========================================
# Database Configuration
# ===========================================
# One of: mongodb | mysql | postgresql
DATABASE_TYPE=mongodb
# Connection string (for MongoDB default; MySQL uses DATABASE_URL_MYSQL)
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/database"
# Optional: for MySQL
# DATABASE_URL_MYSQL="mysql://user:pass@host:3306/db"
# Optional: for PostgreSQL (schema uses DATABASE_URL_POSTGRES)
# DATABASE_URL="postgresql://user:pass@host:5432/db"

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=info

# ===========================================
# CORS Configuration
# ===========================================
FRONTEND_URL=http://localhost:3000,http://localhost:5173

# ===========================================
# Security & Authentication (required in production)
# ===========================================
COOKIE_SECRET=your-super-secret-cookie-key-change-in-production
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=1d

# ===========================================
# SSL/HTTPS
# ===========================================
USE_HTTPS=false

# ===========================================
# API URL (Production / Render)
# ===========================================
# On Render, leave unset to use RENDER_EXTERNAL_URL automatically
# API_URL=https://your-api.onrender.com

# ===========================================
# Redis (optional – for caching)
# ===========================================
# Leave empty to use in-memory cache (fine for local dev). Production: use Redis URL.
# REDIS_URL=redis://localhost:6379
# REDIS_URL=redis://default:password@host:6379
# REDIS_URL=rediss://... (TLS)

# ===========================================
# Socket.IO (live updates & chat)
# ===========================================
# Uses same HTTP server and port. CORS uses FRONTEND_URL (and API_URL in prod).
# No extra env needed; connect from UI to same host (e.g. http://localhost:8000).

# ===========================================
# Optional: Mail (SMTP), Firebase, ImageKit (see env.config.ts)
# ===========================================
# Gmail SMTP (defaults: SMTP_HOST=smtp.gmail.com, SMTP_PORT=587)
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-gmail-app-password
# MAIL_FROM_NAME=Live Bhoomi
# Optional: MAIL_FROM_EMAIL=your-email@gmail.com (defaults to SMTP_USER)
# For other SMTP servers, set SMTP_HOST and SMTP_PORT.
# FIREBASE_*= IMAGEKIT_*=

# ===========================================
# Google Maps (optional – for address autocomplete, place details, geocoding)
# ===========================================
# Enable Maps REST: GET /api/v1/maps/autocomplete, /maps/place-details, /maps/geocode
# Create key: Google Cloud Console → APIs & Services → Credentials → Create API Key
# Enable: Places API, Geocoding API. Restrict key (HTTP referrers / IP) in production.
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
